# Ink 主题样式缺点分析

本文档基于对 `themes/ink` 当前代码的逐行审查写成，覆盖 `source/css/style.css`（2159 行）、`source/js/main.js` 与全部 EJS 模板。文中每条缺点均给出代码位置（`文件:行号`），便于核对与修复。

主题的整体底子不差：有设计令牌、有模块化字阶、有 reduced-motion 与 focus-visible。问题不在于"没有"，而在于"不一致"和"半成品"——同一套东西做两遍、做一半、或只在深色模式下成立。以下按系统分层展开。

## 一、色彩系统：深色是亲生的，浅色是领养的

这是全主题最大的硬伤。主题的视觉身份建立在深色之上，浅色模式只做完了表面，经不起任何一组对比度实测。

**1. 强调色在浅色模式下完全没有重定义**

`:root[data-theme="light"]` 只覆盖了背景、文字与渐变（`style.css:98-110`），而 `--color-cyan: #00d4ff`、`--color-magenta: #ff0080`、`--color-green: #00ff88` 原样保留（`style.css:22-24`）。后果：

- 链接青色 `#00d4ff` 在 `#fafafa` 背景上对比度约 2.2:1，远低于 WCAG AA 的 4.5:1，浅色模式下正文链接近乎不可读；
- 行内代码的绿色 `#00ff88` 在白底上对比度约 1.6:1，等于看不见；
- 标签洋红 `#ff0080` 上叠白字（hover 态，`style.css:1039-1043`）对比度约 3.9:1，对 12px 小字不合格。

浅色模式不是"换个底色"就完事的。强调色的明度必须随底色反向调整，这是双主题设计的基本功。

**2. 硬编码的深色穿透了主题变量**

- 头部背景 `rgba(10, 10, 15, 0.7)`（`style.css:298`）——浅色模式下毛玻璃导航栏仍是深色的；
- 搜索遮罩 `rgba(10, 10, 15, 0.7)`（`style.css:1410`）——同样的问题；
- 分类树引导线 `--tax-guide-color: #d9d9d9`（`style.css:1693`）——这是浅色 UI 库的灰色，写死在深色主题里，只有浅色模式有一条覆盖（`style.css:1704`），深色下这条亮灰线格外刺眼；
- 代码块 macOS 三色点 `#ff5f57 / #febc2e / #28c840`（`style.css:947-949`）——两套主题通用，均未令牌化。

**3. 阴影令牌没有浅色版本**

`--shadow-card` / `--shadow-card-hover`（`style.css:79-80`）是为深色底设计的浓重黑影（`rgba(0,0,0,0.4~0.6)`），浅色模式下卡片像泡在墨水里。hover 阴影里的青色描边 `rgba(0,212,255,0.3)` 在白底上同样违和。

**4. 派生色靠人肉复制 rgba 字面量**

`rgba(0, 212, 255, ...)` 的不同透明度版本在文件里出现了十余次（0.08、0.12、0.15、0.25、0.3、0.4、0.45……），没有任何一次用 `color-mix()` 或令牌派生。改一次品牌色要全文搜索替换，漏一处就是视觉 bug。2024 年后的 CSS 完全有条件用 `color-mix(in oklch, var(--color-cyan), transparent 70%)` 收敛这类派生。

**5. 死令牌**

`--glow-cyan`、`--glow-magenta`（`style.css:77-78`）定义后从未使用；`--tax-row-h` 与 `--tax-row-h-num` 并存（`style.css:1692, 1698`），后者才是实际生效的。死令牌比没有令牌更糟——它承诺了一个不存在的扩展点。

## 二、设计令牌：两套命名并存的烂尾迁移

`:root` 里同时存在 `--color-*` 新命名和 `--bg / --fg / --cyan` 旧命名，靠一层别名兼容（`style.css:82-95`，注释自己写着 "keep legacy names working during migration"）。迁移没有完成：

- `body` 用的是旧令牌 `var(--bg)` / `var(--fg)`（`style.css:143-144`）；
- 文件前半段（约 1-1230 行）混用新旧两套，后半段（"batch 1" 追加部分起）只用新令牌；
- 结果是改主题色时无法判断该改哪一层——改新令牌，用旧别名的组件不一定跟上。

令牌系统的价值在于"单一事实来源"。双轨并行等于没有事实来源。

## 三、字体与排版：方向正确，细节翻车

**1. 字体依赖 Google Fonts，对中文站是结构性风险**

四个字体族、合计 18 个字重全部从 `fonts.googleapis.com` 远程加载（`head.ejs:45-50`）。对一个面向中文读者的博客，这意味着：

- Google Fonts 在国内访问不稳定，首屏字体可能整体超时回退；
- 未自托管，存在隐私合规问题（GDPR 语境下的数据出境）；
- `head.ejs:48` 用 `preload` 预载了一份字重子集的 CSS，`head.ejs:50` 又加载完整版——两个 URL 不同，浏览器不会去重，预载纯属浪费带宽。

**2. 全局 `font-feature-settings` 是 cargo cult**

`body` 上写了 `font-feature-settings: "cv11", "ss01", "ss03"`（`style.css:149`）。这三个是 Inter 私有的 OpenType 特性开关，对 Noto Sans SC、PingFang、微软雅黑全部无效——而它们恰恰排在字体栈的回退位。这行代码只对"恰好装好了 Inter"的用户有效，却加在了全局。

**3. 负字距用在中文标题上**

`.hero-title` 设了 `letter-spacing: -0.04em`（`style.css:546`），各级标题 `-0.015em ~ -0.03em`。负字距是西文展示字体的手法；汉字是等宽方块字，收紧字距只会让笔画粘连，字号越大越明显。中文标题的正确做法是字距为 0 或微正。

**4. "模块化字阶"名不副实**

注释宣称 ratio 1.2（`style.css:38`），实际比值在 1.176 ~ 1.25 之间漂移（1.0625→1.25 是 1.176，1.5→1.875 是 1.25）。要么把注释改诚实，要么把数值算准——声称有系统而实际没有，比没有系统更误导维护者。

**5. `:lang(en)` 规则是死代码**

`.post-body p:lang(en) { line-height: 1.7 }`（`style.css:839`）。整站 `html lang` 是 `zh-CN`，正文段落不会被标上 `lang="en"`，这条规则永远不会命中。同理 `:lang(zh-Hans)`（`style.css:820`）匹配不到 `zh-CN`——这条是写错的（`:lang(zh)` 按前缀匹配没问题，`-Hans` 后缀才是问题）。

**6. 中文首行缩进一刀切**

`.post-body p:lang(zh)` 统一 `text-indent: 2em`（`style.css:819-822`），只豁免了标题后首段，没有豁免引用块、列表项内的段落。引用块里的段落缩进 2em，左侧竖线 + 缩进的组合视觉上很邋遢。

**7. 斜体用在中文上**

`.empty` 空状态用了 `font-style: italic`（`style.css:2071`）。中文字体没有真斜体，浏览器会强制倾斜（faux italic），笔画变形，是中文排版的典型低级错误。

**8. `<strong>` 被设计成了荧光笔**

所有加粗文字都被套上洋红半透明下衬（`style.css:866-871`）。加粗是一个通用语义标记，可能出现在任何语境（术语、警告、人名）；把它一律渲染成"划重点"的荧光笔效果，是把一种具体修辞强加给所有强调。

## 四、链接与交互：基础样式太凶，后面全程在灭火

**1. 全局链接样式与全文件为敌**

`a` 的默认样式带相对定位 + 渐变下划线生长动画（`style.css:223-236`）。这套样式对正文链接很优雅，但它加在了**所有** `<a>` 上，于是文件后半部分出现了至少 15 处 `background: none` / `background-image: none` 的覆盖（导航、卡片标题、目录、分页、标签、面包屑、相关文章……），目录链接甚至动用了 `!important`（`style.css:417, 446`）。

正确做法是反过来：默认链接零装饰，只对 `.post-body a` 施加美化。现在的写法是"先污染、后治理"，每加一个新组件都要记得灭火。

**2. `a { position: relative }` 污染全局层叠**

`style.css:226` 给所有链接加了相对定位，这会为每个链接创建新的层叠上下文，波及全站所有 z-index 计算。而它服务的下划线动画根本不需要这个定位（用的是 background，不是伪元素）。

**3. `a:active { opacity: 0.6 }` 是全局闪光弹**

`style.css:1380` 让所有链接——包括整张大卡片、导航项、页码——在点按时整体变透明。卡片标题闪一下可以接受，导航栏闪一下就是噪声。

**4. `transition: all` 泛滥**

分页、标签、分享按钮、主题切换、分类行等十余处用了 `transition: all`（如 `style.css:254, 735, 1035, 1535, 1573, 1668`）。`all` 会把将来新增的任何可动画属性也拖进过渡，是 CSS 里的"隐式契约"，属于已知反模式，应明确列出属性。

**5. 触摸设备上的 hover 依赖**

锚点链接 `#` 只在标题 hover 时显现（`opacity: 0` → hover 显现，`style.css:1338-1355`）；图片放大只有 `cursor: zoom-in` 提示（`main.js:126`）。触摸屏没有 hover，这些功能在移动端是不可发现的。hover 可以是增强，不能是唯一入口。

## 五、动效：永动的装饰，脆弱的内容

**1. 无限动画过多且互相叠加**

- logo 渐变流动 8s 无限循环（`style.css:328-334`）；
- 同一动画复用在 hero 强调字、页面标题强调字、404 大数字、密码门标题上（`style.css:550-557, 526-533, 2093-2099, 1116-1120`）；
- hero 状态点 2s 呼吸（`style.css:488-491`）。

页面上同时存在 3-4 个永动元素，视线被持续拉扯。编辑型设计的动效原则是"动一次，静下来"，现在的做法是"永远在动"。而且同一招（三色渐变 + background-position 流动）重复了五处——再贵的招，用五次也不贵了。

**2. reveal 动画没有 no-JS 兜底**

`.reveal` 默认 `opacity: 0`，靠 JS 的 IntersectionObserver 加 `.in`（`style.css:1178-1186`, `main.js:342-354`）。JS 被禁用、加载失败、或被密码门拦下时，整页内容永久隐形。渐隐入场应该是"JS 加上 `.reveal` 再观察"，而不是"CSS 先藏起来等 JS 救"。

**3. `max-height: 5000px` 展开动画是教科书级反模式**

分类树用 max-height 从 0 到 5000px 做过渡（`style.css:1920-1927`）。当真实内容只有 200px 时，动画 95% 的时间浪费在不可见的区间，展开表现为"先延迟、后瞬开"，收起表现为"先瞬收、后延迟"。这正是这个 hack 的著名症状。现代写法是 CSS Grid 的 `grid-template-rows: 0fr → 1fr`。

**4. 猜测性 CSS 留在生产代码里**

`padding-left: calc(var(--tax-indent) * attr(data-depth number, 0))`（`style.css:1724`）——带类型推断的 `attr()` 写法在绝大多数浏览器里尚未落地，注释也承认要靠下面的属性选择器兜底。不生效的代码应当删除，而不是留在文件里冒充未来。

## 六、布局与响应式：三个断点管不了所有设备

**1. 文章页在平板和手机上没有目录**

1100px 以下侧边栏从 sticky 变为静态、甩到正文之后（`style.css:1189-1201`）。也就是说，中长文在最需要导航的小屏设备上，目录出现在文章末尾——等于没有。主题没有提供任何小屏目录方案（折叠面板、浮动按钮、顶部下拉均无）。

**2. 文章页视觉重心偏离视口中心**

`--content-max: 1440px` 的容器里放 `300px` 侧栏 + `940px` 阅读栏（`style.css:61-63`）。宽屏下正文列在容器内居中，但容器本身含侧栏，导致正文相对视口偏右约 150px，左侧空、右侧挤。阅读型页面要么正文对视口居中，要么侧栏做成真正贴边的"边注"。

**3. `overflow-x: hidden` 掩盖问题**

`body` 上全局 `overflow-x: hidden`（`style.css:152`）。横向溢出通常是具体组件的 bug（长 URL、宽表格、负 margin），全局隐藏等于把症状压下去，让用户失去横向滚动的同时还看不清是谁溢出。应当逐一定位溢出源。

**4. 移动端重定义间距令牌**

720px 以下直接改写 `--space-5` 到 `--space-9` 的值（`style.css:1204-1210`）。令牌的意义是"语义稳定"，同一个 `--space-7` 在桌面是 3rem、移动是 2rem，任何用到它的组件都被迫参与这套缩放——包括那些本来不该缩的。想缩什么就改什么选择器，不要改令牌本身。

**5. 小组件缺移动端规则**

- `.post-nav`（上一篇/下一篇）固定 `1fr 1fr` 两列（`style.css:1957-1959`），没有任何断点处理——360px 屏上两张卡片各约 160px 宽，长标题被挤成 vertical 长条；
- 导航栏没有汉堡菜单，小屏只靠缩小字号和间距硬塞（`style.css:1224-1229`），菜单项一多必溢出；
- 缺少 `@media (hover: none)` 维度，触摸设备沿用为鼠标设计的 36px 按钮（主题切换、搜索），只有零星几处补了 44px（`style.css:1260-1265`）。

**6. 没有 `color-scheme` 声明**

全站未声明 `color-scheme: dark` / `light`。后果是深色模式下浏览器原生 UI——滚动条（Firefox）、表单控件、`<input type="search">` 的清除按钮——仍按浅色渲染，搜索框里会冒出一块亮斑。一行声明就能解决，至今缺席。

## 七、组件细节：完成度参差

**1. 两套目录代码，一套是死的**

`_partial/toc.ejs` 输出 `.toc / .toc-header / .toc-toggle / .toc-body`，但 CSS 里这四个类**零样式**（全文检索无匹配）；`main.js:23-31` 的 `.toc-toggle` 展开逻辑也因此在操作一个无样式的幽灵组件。实际生效的目录是 `sidebar.ejs` 的 `.sidebar-toc`。`toc.ejs` 应当删除——死代码留在主题里，每个接手的人都会以为它活着。

**2. 同一套样式写两遍**

- `.sidebar-toc a` 与 `.sidebar-toc .toc-link` 的声明几乎逐字重复（`style.css:408-447`）；
- 行内代码样式在 `style.css:682-690` 和 `style.css:1275-1285` 定义了两次（数值还略有出入：2px 6px vs 1px 6px）；
- `tabular-nums` 用一个 19 项的超长选择器集中施加（`style.css:842-861`）——每加一个带数字的组件都要记得回这里登记，漏一个就跳字。这活本该由各组件自己声明。

**3. 图标语言不统一**

搜索按钮是 SVG 线条图标（`header.ejs:15`），主题切换是 emoji 🌙/☀️（`header.ejs:21-22`），密码门是 emoji 🔒（`layout.ejs:50`, `article.ejs:28`）。emoji 的渲染随操作系统漂移（Windows 上是色块，macOS 上是插画），和精心设计的三色渐变、毛玻璃完全不在一个视觉体系里。全站应统一为同一套 SVG 图标。

**4. 代码块 macOS 三色点是视觉噱头**

给每个代码块戴上红黄绿三个窗口按钮（`main.js:41-46`）既不能点也没有信息，属于 2015 年博客主题的残留审美。语言标签 + 复制按钮已经足够，三色点可以去掉，或者至少令牌化。

**5. 标签容器没有样式**

`article.ejs:50-56` 输出的 `.post-footer` 与 `.post-tags` 在 CSS 中无任何规则，标签群与正文之间没有间距保障，全靠浏览器默认 margin 碰运气。

**6. 自定义滚动条只有 WebKit 一半**

渐变滚动条（`style.css:168-175`）只对 Chrome/Edge/Safari 生效；Firefox 需要 `scrollbar-color`/`scrollbar-width`，未提供。要么补齐，要么接受差异——但渐变滚动条本身对可用性就是负分（拇指与轨道的对比度随位置变化，位置感变差）。

**7. z-index 没有标尺**

9998（搜索）/ 9999（进度条、密码门）/ 10000（skip-link、lightbox）随手取值（`style.css:199, 1090, 1401, 208`; `main.js:102`）。进度条压在搜索浮层上面、skip-link 和 lightbox 平级——目前没有撞车纯属运气。应当建立分层约定（如 10/100/1000/9000 各对应一层语义）。

**8. `.tax-check` 声明自相矛盾**

`width/height: 18px` 与 `min-width/min-height: 36px` 同时写（`style.css:1839-1844`），实际生效的是后者，前者是误导。想做大点击热区应该写成 18px 视觉圆 + 36px 透明热区（用伪元素或 padding），而不是两条互相打架的声明。

## 八、性能相关的样式问题

**1. 固定背景渐变在移动端是滚动杀手**

`body` 的背景用了两层径向渐变 + `background-attachment: fixed`（`style.css:154-157`）。fixed 背景在移动端 Safari 会强制每帧重绘整页，是经典的滚动掉帧源。

**2. 毛玻璃导航全程工作**

`backdrop-filter: saturate(180%) blur(20px)`（`style.css:299`）意味着滚动经过导航下方的每一像素都要实时模糊合成，低端 Android 上可感知发热掉帧。可以降低 blur 半径，或在滚过一定距离后切换为不透明背景。

**3. 图片没有尺寸预留**

正文图片只有 `max-width: 100%; height: auto`（`style.css:1300-1310`），没有 `aspect-ratio` 占位；`main.js:91-94` 还给**所有**图片（包括首屏第一张）无差别加上 `loading="lazy"`。前者造成 CLS（布局偏移），后者拖慢 LCP（最大内容绘制）——首图应当 `loading="eager"` + `fetchpriority="high"`。

**4. 代码高亮主题写死**

highlight.js 的 `github-dark` 主题直接引自 CDN 且只有一份（`head.ejs:43`）。浅色模式下代码块仍是一块深色补丁，且样式表依赖 jsDelivr 可用性。双主题需要双份高亮 CSS（或用 CSS 变量重定义高亮色板）。

## 九、工程结构：按"批次"生长的单文件

`style.css` 的中间位置有一行诚实的注释：`A11y, motion, and detail polish — appended in batch 1`（`style.css:1232`）。这暴露了文件的生长方式：新需求不找归属，直接追加到文件尾。症状全在上面：重复定义、双轨令牌、互相覆盖、死代码。2159 行单文件没有构建工具并不是问题——Hexo 主题不需要——但**文件内部必须有秩序**：按"令牌 → 基础 → 布局 → 组件 → 页面 → 工具"分区，同一组件的规则收在一处。现在同一个"链接"的规则散落在至少 12 个位置。

JS 侧同样存在样式外流：lightbox 的全部样式写在 `main.js:102-103` 的内联 `cssText` 里，无法被 CSS 覆盖、无法适配主题、无法走缓存。DOM 由 JS 创建没问题，样式必须回 CSS 文件。

## 十、优先级建议

按"用户可感知程度 × 修复成本"排序：

**P0 — 浅色模式还债**（第一大节全部）。要么把强调色、阴影、硬编码深色全部令牌化并重调，要么干脆砍掉浅色模式只留深色——一个残废的浅色模式比没有浅色模式更伤专业感。

**P1 — 健壮性底线**。reveal 的 no-JS 兜底（五.2）、`:lang(en)` 死代码与缩进豁免（三.5、三.6）、`color-scheme` 声明（六.6）、删除 `toc.ejs` 死组件（七.1）。

**P2 — 链接体系重构**（第四节）。反转默认链接样式、消灭全部 `background: none` 覆盖和 `!important`。

**P3 — 动效收敛**（第五节）。无限动画只留一处（logo），其余改为入场一次后静止；max-height 改 Grid 展开。

**P4 — 响应式补齐**。小屏目录方案、post-nav 单列断点、`(hover: none)` 维度的触控尺寸。

**P5 — CSS 文件秩序化**（第九节）。在不动视觉的前提下纯重组，消除重复与死令牌。放在最后，因为前面几步每改一处都会顺带收拾一处。

最后一句总评：这套主题的审美方向（暗色编辑型 + 电光三色）是成立的，令牌、字阶、a11y 的"意识"也都在。它真正缺的不是品味，而是**完成度纪律**——每一条系统（双主题、令牌、链接、动效、响应式）都停在了 70%。把任何一条从 70% 推到 95%，带来的专业感提升都会超过再加一个新特性。

---

# 修复记录（2026-09-07）

以上缺点已逐条修复。修复通过 `hexo generate` 全量构建（203 文件）与 `hexo server` 运行时抽查验证。以下按章节对账。

## 一、色彩系统 —— 已全部修复

1. **浅色模式强调色**：`--color-cyan → #0070b8`（约 4.9:1）、`--color-magenta → #d60060`（约 5.2:1）、`--color-green → #067647`（约 5.0:1），全部过 WCAG AA；标签 hover 改为浅色晕染，不再有白字叠洋红。
2. **硬编码深色令牌化**：新增 `--color-header-bg`、`--color-overlay-bg`；分类树引导线改用 `--color-border-strong`；macOS 三色点整体删除。
3. **浅色阴影**：`--shadow-card / --shadow-card-hover / --shadow-overlay` 三套阴影均有浅色版本。
4. **派生色**：全部 rgba 字面量改为 `color-mix(in srgb, var(--color-*) N%, transparent)`。
5. **死令牌**：`--glow-*`、`--tax-row-h-num` 已删除。

## 二、设计令牌 —— 已修复

旧命名（`--bg / --fg / --cyan` 等）与别名层整体删除，全站统一 `--color-*` 一套命名。新增 z-index 标尺（`--z-header:100 → --z-skiplink:500`）。

## 三、字体与排版

1. **字体加载**：删除重复的 preload（原为两份不同 URL，浏览器不去重）；字重从 18 个裁到实际使用的 14 个（JetBrains Mono 去掉 500/700、Noto Sans SC 去掉 900）。**诚实说明**：字体仍从 Google Fonts 远程加载——自托管四族中文字体需引入数 MB 二进制与构建期子集化，属于基础设施决策，未在本次样式修复中代做。
2. **`font-feature-settings`**：已删除。
3. **负字距**：标题一律 `letter-spacing: 0`。
4. **字阶**：按 1.2 比率从 17px 基准重算（0.75 / 0.9 / 1.0625 / 1.275 / 1.53 / 1.836 / 2.2 / 2.64rem）。
5. **死规则**：`:lang(en)`、`:lang(zh-Hans)` 删除。
6. **首行缩进**：新增引用块、列表项内段落豁免。
7. **`.empty` 斜体**：已删除。
8. **`<strong>` 荧光笔**：恢复为常规加粗。

## 四、链接与交互 —— 已全部修复

- 基础 `a` 回归安静（仅颜色过渡）；下划线处理只在 `.entry-content / .post-body` 内生效；全部 `background: none` 覆盖与 `!important` 随之删除。
- `position: relative`、`a:active { opacity }` 删除；按压反馈只保留在真实按钮上。
- 全部 `transition: all` 改为显式属性列表（已 grep 验证为零）。
- 锚点链接增加 `:focus-visible` 显现；`(hover: none)` 设备上常显（0.6 透明度）。

## 五、动效 —— 已全部修复

- 无限动画只保留 logo 的 `logo-shimmer`；hero/page-hero/404/密码门的强调字改为静态渐变，hero 呼吸点改为静态光点。
- reveal 改由 `.js` 类门控（`head.ejs` 内联脚本在绘制前加类）：无 JS 时内容天然可见。
- 分类树展开改为 `grid-template-rows: 0fr → 1fr`（`category.ejs` 增加 `.tax-children-inner` 单子包裹层），删除 `max-height: 5000px`。
- `attr(data-depth number)` 猜测性规则删除。

## 六、布局与响应式 —— 已全部修复

1. **小屏目录**：`sidebar.ejs` 改为 `<details>/<summary>` 结构——桌面端 summary 渲染为静态"目录"标题且内容强制可见；≤1100px 变为文章上方的折叠目录面板（原生交互、无 JS 依赖）。
2. **文章页重心**：`.content-wrapper.is-detail` 限宽为侧栏+阅读栏+间距之和，整组居中。
3. `overflow-x: hidden → clip`（不再创建滚动容器，sticky 不受影响）。
4. 移动端重定义间距令牌的做法已删除，改为直接调整组件。
5. `.post-nav` 增加 ≤720px 单列断点；新增 `(hover: none)` 触控尺寸块（图标按钮 44px）。
6. **`color-scheme`**：`dark` / `light` 已按主题声明，原生控件与滚动条跟随主题。

## 七、组件细节 —— 已全部修复

1. `_partial/toc.ejs` 死文件删除，`main.js` 里的死 TOC 代码同步删除。
2. `.sidebar-toc a` / `.toc-link` 重复规则合并为一条；行内代码双重定义合并；`tabular-nums` 登记表解散、并入各组件自身规则。
3. 主题切换 🌙/☀️、密码门 🔒 全部换为 SVG 线条图标；搜索与主题按钮统一为 `.icon-btn` 组件。
4. macOS 三色点删除，代码块头部只留语言标签 + 复制按钮（新增 `.code-header-side`）。
5. `.post-footer` / `.post-tags` 补上间距与布局；详情页尾部各块（标签、meta、分享、上下篇、相关文章）统一对齐 `--reading-max`。
6. Firefox 滚动条补 `scrollbar-color` / `scrollbar-width`；WebKit 滚动条改为克制的中性色（渐变拇指删除）。
7. z-index 全面改用标尺令牌。
8. `.tax-check` 矛盾声明修正为 36px 点击热区 + 16px 视觉圆。

## 八、性能 —— 已修复

1. `background-attachment: fixed` 删除，渐变背景改为 `body::before` 固定合成层。
2. 导航毛玻璃 `blur(20px) → blur(12px)`，去掉 `saturate(180%)`。
3. 首图 `loading="eager" + fetchpriority="high"`，其余图片懒加载；`cursor: zoom-in` 从 JS 内联收回 CSS。**说明**：`aspect-ratio` 占位需要 Markdown 渲染期输出宽高属性，超出本次范围。
4. highlight.js 的 CDN github-dark 替换为本地 `css/hljs.css`——变量驱动、随明暗主题自动翻转、零外部依赖。

## 九、工程结构 —— 已修复

`style.css` 按"令牌 → 基础 → 布局 → 头部 → 组件 → 页面 → 工具 → 响应式 → 打印"重排为 32 个编号分区，文件头注明秩序约定；"batch 1"注释、重复定义、死代码全部清除。lightbox 样式从 `main.js` 内联 `cssText` 迁回 CSS（`.lightbox` / `.lightbox-img` / `.lightbox-close`，`hidden` 属性控制显隐）。

## 验证方式

- `npx hexo generate`：203 文件全量构建通过；
- `hexo server` 抽查：首页、分类页、文章页、两份 CSS 均 200，页面含新 markup（`icon-btn`、`sidebar-toc-details`、`tax-children-inner`、SVG 锁图标）；
- `node --check` 校验 `main.js`；CSS 花括号配平校验通过；
- grep 验证：旧令牌别名、`transition: all`、硬编码 `rgba(0,212,...)`、`.toc-toggle` 死类、`!important`（除 reduced-motion/print/工具类的合法用途）均为零残留。

## 遗留事项（本次有意未做）

- **字体自托管与子集化**：需要引入字体二进制与构建流程，建议作为独立任务决策。
- **图片 `aspect-ratio` 占位**：需要渲染期输出图片宽高（可用 marked 自定义 renderer），属内容管线改造。
- **导航汉堡菜单**：当前菜单仅 4 项，小屏缩小后可容纳；菜单项增多时再补。
